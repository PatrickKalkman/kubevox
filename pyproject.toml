[project]
name = "kubevox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11.12",
    "kubernetes>=32.0.0",
    "loguru>=0.7.3",
]
